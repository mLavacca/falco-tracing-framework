record:
  prog_config: 
    prog_bin: /home/mattia/Desktop/sysdig/build/userspace/sysdig/sysdig
    prog_args:
      - "-w"
      - "/tmp/falco_tester.scap"
  breaking_profile: interleaved2
    
report:
  prog_config: 
    prog_bin: /home/mattia/Desktop/falco/build/userspace/falco
    prog_args:
      - "-c"
      - "falco.yaml"
      - "-e"
      - "/tmp/falco_tester.scap"
      - "-o" 
      - "json_output=True"
      - "-o"
      - "json_include_output_property=True"
      - "-o"
      - "priority=debug"
      - "-v"
  iterations: 100

rules_breakers:
  - rule: Error File below / or /root opened for writing
    rule_id: 0

  - rule: Write below etc
    rule_id: 1

breaking_profiles:
  - name: flat
    sequence: [[0], [1]]
    rollback_sequence: [0, 1]
    ratio: 5
    limit: 1000

  - name: interleaved1
    sequence: [[0, 1]]
    rollback_sequence: [0, 1]
    ratio: 1
    limit: 1000

  - name: interleaved2
    sequence: [[0,1,0], [1,0,1]]
    rollback_sequence: [0, 1]
    ratio: 10
    limit: 10 
